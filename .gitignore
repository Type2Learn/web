# Local and deployment secrets must never be committed or published by the
# static site host. Keep non-secret templates under an explicit *.example name.
security/api.env
.env
.env.*
!.env.example
!*.env.example

# Local test output
test-results/
playwright-report/
screenshots/
node_modules/
